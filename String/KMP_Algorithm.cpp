#include <bits/stdc++.h>
using namespace std;
void find(char text[],char pattern[],int n){
    int m = strlen(text);
    int m = strlen(pattern);
    int t = 0;
    int p = 0;
    int h = 1;
    for(int i=0;i<m-1;i++){
        h=(h*d)%q;
    }
}