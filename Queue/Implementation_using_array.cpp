#include <bits/stdc++.h>
using namespace std;
int queue[100] , n=100 , front = -1 , rear = -1;
void insert(){
    int val;
    if(rear == n-1){
        cout<<"Overflow";
    }
    else{
        if(front==-1)
        front = 0;
        cout<<"inser"
    }
}